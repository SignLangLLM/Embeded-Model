# Embeded-Model

Clone 후, 영상을 input_video에 넣기

python 3.9 설치 필요

py -3.9 -m venv .venv

.\.venv\Scripts\activate

pip install numpy opencv-python mediapipe google-generativeai Pillow requests urllib3

pip install mediapipe sentence-transformers scikit-learn numpy

python run_pipeline.py
